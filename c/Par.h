#include <iostream>
using namespace std;

struct Par {
  int x;
  int y;
};

Par consPar(int x, int y);
int fst(Par p);
int snd(Par p);
int maxDelPar(Par p);
Par swap(Par p);
Par divisionYResto(int n, int m);
void ShowPar(Par p);
